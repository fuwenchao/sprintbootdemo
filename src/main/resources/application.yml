spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wenchao
    username: root
    password: 900525f
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true